:root {
}

.ui-button {
    --bg: rgb(0, 0, 0);
    --font-size: 16px;
    background-color: var(--bg);
    font-size: var(--font-size);
    width: auto;
    height: auto;
    flex-grow: 1;
    -unity-font-style: normal;
    justify-content: flex-start;
    align-items: stretch;
}
